# Frontend

Pure React + Vite frontend.

## Run

```bash
npm install
npm run dev
```

The frontend expects the FastAPI backend to run on `http://localhost:8000`.
Vite proxies `/api/*` requests to the backend.
